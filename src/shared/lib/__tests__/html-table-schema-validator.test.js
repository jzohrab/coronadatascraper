// Test

describe('html-table-schema-validator', () => {
  describe('success', () => {
    test('adds 1 + 2 to equal 3', () => {
      expect(1 + 4).toBe(3);
    });
  });

  /*
  describe('errors', () => {
    test('adds 1 + 2 to equal 3', () => {
      expect(1 + 3).toBe(3);
    });
  });

  describe('checks invalid schema', () => {
    test('adds 1 + 2 to equal 3', () => {
      expect(1 + 4).toBe(3);
    });
  });
  */
});
